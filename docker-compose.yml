services:
  db:
    image: postgres:15-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: app_user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: finance_calculator
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U app_user -d finance_calculator"]
      interval: 5s
      timeout: 5s
      retries: 5

  migrate:
    build: ./backend
    entrypoint: ["goose"]
    command:
      [
        "-dir", "/migrations",
        "postgres",
        "postgres://app_user:password@db:5432/finance_calculator?sslmode=disable",
        "up"
      ]
    volumes:
      - ./backend/internal/db/migrations:/migrations
    depends_on:
      db:
        condition: service_healthy

  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      POSTGRES_CONNECTION_STRING: postgres://app_user:password@db:5432/finance_calculator?sslmode=disable
      ACCESS_SECRET: DEVENVIRONMENTSECRET
      REFRESH_SECRET: DEVENVREFRESHSECRET
      ENV: develop
      ALLOWED_ORIGIN: http://localhost:3000
    depends_on:
      migrate:
        condition: service_completed_successfully

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  postgres_data:
